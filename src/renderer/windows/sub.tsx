import * as components from '@components'
import * as React from 'react'
import * as ReactDOM from 'react-dom'

const HTMLElement = document.getElementById('root')

ReactDOM.render(<components.SubView />, HTMLElement)
